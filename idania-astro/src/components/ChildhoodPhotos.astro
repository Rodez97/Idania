---
// Fotos especiales de Idania de niña
---

<div class="childhood-section">
    <p class="intro-text">
        Mirar estas fotos tuyas de niña me hace sonreír. Puedo ver en tus ojos la misma luz que veo ahora,
        esa sonrisa genuina que siempre has tenido. Eras hermosa entonces, y sigues siendo hermosa ahora.
    </p>

    <div class="photos-grid">
        <div class="polaroid">
            <div class="polaroid-img">
                <img src="/images/special_1.png" alt="Idania de niña" />
            </div>
            <div class="polaroid-caption">
                Tu sonrisa de siempre ♡
            </div>
        </div>

        <div class="polaroid">
            <div class="polaroid-img">
                <img src="/images/special_2.png" alt="Idania de niña" />
            </div>
            <div class="polaroid-caption">
                Tus ojos brillando ✨
            </div>
        </div>

        <div class="polaroid">
            <div class="polaroid-img">
                <img src="/images/special_3.png" alt="Idania de niña" />
            </div>
            <div class="polaroid-caption">
                Siempre hermosa 💕
            </div>
        </div>
    </div>

    <p class="outro-text">
        Me encanta conocer cada parte de tu historia, desde la niña que fuiste hasta la mujer increíble que eres hoy.
    </p>
</div>

<style>
    .childhood-section {
        margin: 40px 0;
    }

    .intro-text, .outro-text {
        font-size: 1.15em;
        line-height: 1.8;
        color: #555;
        text-align: center;
        margin: 30px auto;
        max-width: 800px;
        font-style: italic;
    }

    .photos-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 40px;
        margin: 50px 0;
        justify-items: center;
    }

    .polaroid {
        background: white;
        padding: 15px;
        padding-bottom: 45px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        border-radius: 4px;
        transition: all 0.3s ease;
        max-width: 300px;
        position: relative;
    }

    .polaroid:nth-child(1) {
        transform: rotate(-3deg);
    }

    .polaroid:nth-child(2) {
        transform: rotate(2deg);
    }

    .polaroid:nth-child(3) {
        transform: rotate(-2deg);
    }

    .polaroid:hover {
        transform: rotate(0deg) scale(1.05);
        box-shadow: 0 15px 50px rgba(0, 0, 0, 0.3);
        z-index: 10;
    }

    .polaroid-img {
        width: 100%;
        aspect-ratio: 3/4;
        overflow: hidden;
        background: #f5f5f5;
    }

    .polaroid-img img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
    }

    .polaroid-caption {
        position: absolute;
        bottom: 15px;
        left: 0;
        right: 0;
        text-align: center;
        font-family: 'Indie Flower', cursive;
        font-size: 1.1em;
        color: #d63447;
        font-weight: 600;
    }

    @media (max-width: 768px) {
        .photos-grid {
            grid-template-columns: 1fr;
            gap: 30px;
        }

        .polaroid {
            max-width: 280px;
        }

        .polaroid:nth-child(1),
        .polaroid:nth-child(2),
        .polaroid:nth-child(3) {
            transform: rotate(0deg);
        }

        .polaroid:hover {
            transform: scale(1.02);
        }
    }
</style>
